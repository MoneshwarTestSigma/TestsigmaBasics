public class Main {
    public static void main(String[] args) {
        Singleton obj=Singleton.getInstance();
        obj.getDetails();
        //For multithreading environment we need to have
    }
}